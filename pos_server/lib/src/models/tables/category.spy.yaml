class: Category
table: category
fields:
    # Client
    name: String
    storeId: int, relation(parent=store)
    merchantId: UuidValue

indexes:
    category_name_store_idx:
        fields: name, storeId
        unique: true
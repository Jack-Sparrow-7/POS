class: Store
table: store
fields:
    # Client Side
    name: String
    email: String
    merchantId: UuidValue
    type: String?

indexes:
    store_name_merchant_idx:
        fields: name, merchantId
        unique: true
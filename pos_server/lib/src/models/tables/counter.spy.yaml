class: Counter
table: counter
fields:
    # Client
    name: String
    merchantId: UuidValue
    storeId: int, relation(parent=store)

indexes:
    counter_name_store_idx:
        fields: name, storeId
        unique: true